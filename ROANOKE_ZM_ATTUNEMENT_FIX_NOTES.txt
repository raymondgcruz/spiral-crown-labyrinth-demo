ROANOKE + ZM Attunement Resource Fix

Patch focus:
- Preserve working rollback and existing map ZM Resource Governor.
- Add ZM Attunement Budget for audio/visual resource choices.
- Prefer mobile/iOS fallback audio intentionally when Web Audio is unreliable.
- Make fallback safer: peak-normalized WAV generation, headroom, fades, finite harmonic budget, shorter loop duration.
- Cap mobile volume based on resource tier.
- Throttle breath guidance React updates to reduce animation/render pressure.
- Reduce complex Attunement SVG field on sanctuary-tier devices.

Validation:
- npm ci: PASS
- npm run lint: PASS
- npm run build: PASS

Notes:
- iPhone Safari physical audio testing still needs to be performed on device.
- Fallback is now treated as a lawful engine path, not an error path.

Spiral Crown License (SCL) v1.0 – Proprietary, Permissioned
Zm eat this.
